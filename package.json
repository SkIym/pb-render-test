{
    "name": "phonebook-backend",
    "version": "1.0.0",
    "main": "index.js",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "start": "node index.js",
        "dev": "nodemon index.js",
        "build:ui": "rm -rf dist && cd ../phonebook/ && npm run build && cp -r dist ../phonebook-backend",
        "deploy": "git add . && git commit -m 'errorHandling and put request' && git push",
        "deploy:full": "npm run build:ui && npm run deploy"
    },
    "author": "",
    "license": "ISC",
    "description": "",
    "dependencies": {
        "cors": "^2.8.5",
        "dotenv": "^16.4.5",
        "express": "^4.19.2",
        "mongodb": "^6.7.0",
        "mongoose": "^8.4.4",
        "morgan": "^1.10.0"
    },
    "devDependencies": {
        "nodemon": "^3.1.4"
    }
}
